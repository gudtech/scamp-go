# API Dispatcher main HTTP/WS listener
http.listener.0.groups = api
http.listener.0.port = 8443
http.listener.0.aliases = *:8443
http.listener.0.binaryjs = 1

# API Dispatcher download/edi HTTP/WS listener
http.listener.1.groups = download,edi
http.listener.1.port = 8444
http.listener.1.aliases = *:8444
http.listener.1.https_key = ./../fixtures/logger.key
http.listener.1.https_cert = ./../fixtures/logger.crt

http.listener.1.https_key = ./../fixtures/logger.key
http.listener.1.https_cert = ./../fixtures/logger.crt

# scamp test
logger.soa_cert = ./../fixtures/logger.crt
logger.soa_key = ./../fixtures/logger.key

# Generic
bus.address = if:lo
bus.authorized_services = ./../fixtures/authorized_services
# discovery.local_multicast = 1
discovery.cache_path = ./../fixtures/sample_discovery_cache
discovery.auth_localhost = 1
# Limit 2 to save memory and because limit 1 hits a logic bug and spawns no workers for auth service
worker.limit = 2